<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".Screen4Activity" />
        <activity android:name=".Screen3Activity" />
        <activity android:name=".Screen2Activity" />
        <activity android:name=".HeartWalkActivity" />
        <activity android:name=".DailyReflectionActivity" />
        <activity android:name=".GoalTime" />
        <activity android:name=".week1" />
        <activity android:name=".Week2" />
        <activity android:name=".Week3" />
        <activity android:name=".Week4" />
        <activity android:name=".Week5" />
        <receiver android:name=".ReminderReceiver" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>